# ─────────────────────────────────────────────────────────────────────────────
# Peace Protocols — consolidated runtime requirements (v2)
# ─────────────────────────────────────────────────────────────────────────────
# Core dependencies shared by the Unified MCP Bus, the math indexes, and the
# reference tooling. Per-integration extras live in integrations/<name>/requirements.txt
# and are installed on demand when that server is enabled.
#
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# --- Configuration & serialization ---
pyyaml>=6.0            # MCP registry (config/mcp_servers.yaml) + workflow YAML
tomli>=2.0.1 ; python_version < "3.11"   # TOML parsing (stdlib tomllib on 3.11+)

# --- Async HTTP / MCP transports ---
httpx>=0.27.0          # adapter HTTP transport + health probes
websockets>=12.0       # websocket / SSE MCP transports

# --- Data models & validation ---
pydantic>=2.6.0        # typed specs and API request/response models

# --- Service / API layer (bus serve entrypoint + dashboard) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0


# ─────────────────────────────────────────────────────────────────────────────
# ABOS — Agentic Business Operating System (additive layer)
# ─────────────────────────────────────────────────────────────────────────────
# coincurve is OPTIONAL: a C-speed libsecp256k1 binding used for fast BIP-340
# Schnorr keypairs + signing. ABOS ships a pure-Python BIP-340 fallback
# (abos/nostr/crypto.py) so it runs with zero native deps if coincurve is
# unavailable — the fallback is wire-compatible, just slower.
coincurve>=18.0.0     # optional fast Nostr crypto backend (BIP-340 Schnorr)
